<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD with EJS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <main class="w-full h-screen bg-zinc-950 text-white p-10">
        <a href="/read" class="text-blue-500 font-light tracking-tight mb-10 inline-block hover:text-white">Show All Users >></a>
        <h1 class="text-blue-800 text-5xl mb-10">Create User</h1>
        <form action="/create" method="post">
            <input class="px-3 py-2 border-2 border-zinc-700 bg-zinc-900 outline-none rounded-lg mr-2" name="name" type="text" placeholder="Name">
            <input class="px-3 py-2 border-2 border-zinc-700 bg-zinc-900 outline-none rounded-lg mr-2" name="email" type="text" placeholder="Email">
            <input class="px-3 py-2 border-2 border-zinc-700 bg-zinc-900 outline-none rounded-lg " name="dp" type="text" placeholder="Image URL">
            <input class="px-3 py-2 border-none bg-blue-800 rounded-lg ml-5 hover:cursor-pointer" onclick="return checkFields()" type="submit" value="Create User">
        </form>
    </main>

    <script>
        function checkFields(){
            let name = document.querySelector('input[name="name"]').value;
            let email = document.querySelector('input[name="email"]').value;
            let dp = document.querySelector('input[name="dp"]').value;

            if(name == "" || email == "" || dp == ""){
                alert("Please Fill All The Fields !");
            }
        }
    </script>
</body>
</html>